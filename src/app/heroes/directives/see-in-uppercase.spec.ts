import { SeeInUppercase } from './see-in-uppercase';

describe('SeeInUppercase', () => {
  it('should create an instance', () => {
    const directive = new SeeInUppercase();
    expect(directive).toBeTruthy();
  });
});
